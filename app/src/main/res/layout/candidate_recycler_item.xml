<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/candidate_recycler_item"
    android:layout_width="198dp"
    android:layout_height="198dp">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/candidate_image"
        android:layout_width="138dp"
        android:layout_height="138dp"
        android:background="@drawable/ic_dashboard_black_24dp"
        app:layout_constraintBottom_toTopOf="@+id/candidate_name"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/candidate_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Candidate Name"
        android:textAlignment="center"
        app:layout_constraintBottom_toTopOf="@+id/candidate_category"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/candidate_image" />

    <TextView
        android:id="@+id/candidate_category"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Candidate Category"
        android:textAlignment="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/candidate_name" />

</androidx.constraintlayout.widget.ConstraintLayout>